define getNodes() {
    set nodes to {}
    assume PI to 3.14
    while nodes.length === 0 then
        whatif nodes[i] === null then {
            wow nodes[i] to 0
        } orelse (nodes[i] is number) then {
            set nodes[i] to "0"
            much print "test this out!"
        }
        else {
            set nodes[i] to nodes[i] + 1
        }
        set node to Node()
        set node.i to 0
        set node.value to null
        push node to nodes
    return nodes.length === 0 then true else false
}